{
	"name": "moddable-m5chain",
	"version": "1.0.0",
	"license": "MIT",
	"scripts": {
		"web:build": "npm --prefix web run build",
		"web:dev": "npm --prefix web run dev",
		"format": "biome format --write README.md docs examples manifests src tests web/index.html web/styles.css web/README.md biome.json manifest.json package.json package-lock.json tsconfig.json tsconfig.mod.json && npm --prefix web run fix",
		"lint": "biome check README.md docs examples manifests src tests web/index.html web/styles.css web/README.md biome.json manifest.json package.json package-lock.json tsconfig.json tsconfig.mod.json && npm --prefix web run check",
		"test": "node --no-warnings --experimental-strip-types --test tests/*.test.mjs && npm --prefix web test",
		"typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.mod.json"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.5",
		"@moddable/typings": "^9.5.0",
		"typescript": "^7.0.2"
	}
}
