{
	"name": "moddable-m5chain",
	"version": "1.0.0",
	"license": "MIT",
	"scripts": {
		"buzzer-preview:build": "npm --prefix web/buzzer run build",
		"buzzer-preview:dev": "npm --prefix web/buzzer run dev",
		"format": "biome format --write README.md docs examples manifests src tests biome.json manifest.json package.json package-lock.json tsconfig.json && npm --prefix web/buzzer run format",
		"lint": "biome check README.md docs examples manifests src tests biome.json manifest.json package.json package-lock.json tsconfig.json && npm --prefix web/buzzer run lint",
		"test": "node --no-warnings --experimental-strip-types --test tests/*.test.mjs",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.5",
		"@moddable/typings": "^9.0.1",
		"typescript": "^7.0.2"
	}
}
