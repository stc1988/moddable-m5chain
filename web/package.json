{
	"name": "m5chain-web-tools",
	"version": "0.1.0",
	"private": true,
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "vite",
		"build": "tsc --noEmit && vite build",
		"test": "node --no-warnings --experimental-strip-types --test tests/*.test.mjs",
		"preview": "vite preview",
		"check": "biome check .",
		"fix": "biome check --write ."
	},
	"dependencies": {},
	"devDependencies": {
		"@biomejs/biome": "2.5.5",
		"@types/node": "24.10.1",
		"esbuild": "0.28.1",
		"typescript": "5.9.3",
		"vite": "8.2.2"
	},
	"type": "module"
}
